{
  "name": "opencode-claude-auth",
  "version": "1.0.0",
  "description": "OpenCode plugin: Claude CLI token + spoofed headers for Claude Pro/Max.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm && tsc --emitDeclarationOnly",
    "clean": "rm -rf dist",
    "prepare": "bun run build",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["opencode", "plugin", "anthropic", "claude", "oauth"],
  "license": "MIT",
  "dependencies": {
    "@opencode-ai/plugin": "latest"
  },
  "devDependencies": {
    "bun-types": "^1.3.6",
    "typescript": "^5.7.3"
  }
}
